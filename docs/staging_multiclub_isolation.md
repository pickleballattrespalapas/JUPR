# Three clubs for isolation testing

Prepared September 16, 2026 in the isolated staging database. Use the existing
staff sign-in and [club selector](https://jupr-git-staging-pickleballattrespalapas1.vercel.app/admin/select-club).
La Ribera, Cabo and La Paz each have an active administrator assignment bound to
the same verified account. Sign out and back in if a new club is missing.

| Club | Players | Recorded matches | League setup | Tournament setup |
| --- | ---: | ---: | --- | --- |
| La Ribera Pickelball Club | 24 | 60 | Active ladder and editable draft | Four teams and six scheduled games |
| Cabo Test Club | 24 | 60 | Active ladder and editable draft | Four teams and six scheduled games |
| La Paz Test Club | 24 | 60 | Active ladder and editable draft | Four teams and six scheduled games |

All records are synthetic. Each club has 23 active players and one inactive
player, with 20 players who have match history and four reserves/new players.
There are 12 women and 12 men per club. Player names either carry a club prefix
(`LR`, `CB`, `LP`) or end in `[TEST]`. Contact details use a non-deliverable test
domain; no subscriptions or email deliveries were created.

The 60 matches span six weeks. Player and league ratings, win/loss counts and
rating snapshots agree with that history. Ratings use the existing JUPR
calculation. **Isolation Test Ladder**, **Isolation Test Draft**, and
**Isolation Test Cup** deliberately have the same names at all three clubs.
Their records, rosters and IDs are separate.

## Try the club boundaries

1. Open **La Ribera → Players** and find **Alex Rivera [TEST]**. Repeat in Cabo
   and La Paz. The names match, but the player IDs, ratings and match histories
   differ. Six names and six fictional contact addresses are intentionally
   repeated across clubs.
2. Change Alex's rating or active status in one workspace. Switch clubs and
   confirm the other copies retain their previous values. You can also add a
   new player in one club and search for that name in the others.
3. Open **Match Log**, **League Manager**, and **Tournament Manager** after each
   switch. The names of the sample programs match; their participants and
   records must belong to the selected club. Try editing a draft league or a
   tournament team and then checking the other clubs' copies.
4. In a public player or match link, substitute another club's slug while
   keeping the player/match ID. That object must be unavailable. A foreign
   player's match-history endpoint must return no matches.
5. Leave an admin page open in a second tab, switch clubs in the first tab,
   then return to the older tab. Refresh its club context before making edits;
   an old selection must not silently operate in the newly selected club.

Public player directories:

- [La Ribera](https://jupr-git-staging-pickleballattrespalapas1.vercel.app/clubs/la-ribera-pickelball-club/players)
- [Cabo](https://jupr-git-staging-pickleballattrespalapas1.vercel.app/clubs/cabo-test-club/players)
- [La Paz](https://jupr-git-staging-pickleballattrespalapas1.vercel.app/clubs/la-paz-test-club/players)

## Try the interclub boundaries

Open **All club invitations and meet rosters** from Interclub seasons, and select
**Three Club Isolation Test**. La Ribera organizes this sample; all three clubs
have accepted participation. The user's Southern BCS setup and existing Tres
players, matches, leagues, tournaments and season drafts were preserved.

| Meet | Host | Start in Baja California Sur | Divisions |
| --- | --- | --- | --- |
| 1 | La Ribera | October 3, 2026, 8:00 a.m. | 3.5 and 4.0 |
| 2 | Cabo | October 17, 2026, 8:00 a.m. | 3.5 and 4.0 |

Each club has one four-player mixed team per division at each meet. The second
meet substitutes two players per team. Edit a lineup for one meet and verify
that the other meet's lineup stays the same.

La Ribera's organizer view can see the submitted names, starting ratings and
eligibility for all clubs. Cabo and La Paz manage their own teams. Sharing those
submitted facts is intentional; private contact details and full club player
directories must stay out of the organizer's interclub view. Being signed in
with administrator access to all three clubs still lets you explicitly switch
workspaces to manage each one.

## Verification and reproducibility

The fixture is generated by `scripts/prepare_multiclub_isolation_fixture.py` and
`scripts/fixtures/multiclub_isolation.sql`. Generation rejects any project other
than `sijpxjxvdtrehmqvirfi`. The SQL also requires the existing synthetic staging
sentinel. It creates the new clubs through the guarded invitation and verified
acceptance functions. The seed commits atomically, refuses unrelated target
data, and becomes a no-op once the fixture exists, preserving manual edits.

Completed verification includes:

- A rollback-only seed rehearsal, then the committed fixture transaction.
- Exact preservation of existing Tres players, matches, leagues, tournaments,
  and season drafts using before/after database fingerprints.
- Zero cross-club references in match participants, league rosters, player
  contacts, tournament teams and interclub rosters.
- Live guarded-transaction checks: six rejected foreign-player submissions,
  six rejected foreign-team edits, three rejected inactive-player submissions,
  and three successful substitutions that left other teams/meets unchanged.
  Every probe was rolled back.
- Focused service/API tests for separate directories, same-name league rosters,
  scoped player edits, single-club authorization, and interclub contact filtering.

`scripts/verify_multiclub_public_isolation.py` checks the deployed staging API's
club lists, match history, public profiles, same-name league results and foreign
object URLs. It makes GET requests only, checks staging identity, and rejects a
deployment change mid-run. It expects the original fixture counts; manual
testing can legitimately change those counts. The SQL verification script also
expects the original roster revisions and upcoming meets.

## Dedicated automated test administrator

`pcs-staging-qa@example.invalid` is a separate staging Auth identity, with
administrator access only to La Ribera, Cabo and La Paz. It has no platform
administrator assignment and cannot manage Tres. The address is deliberately
non-deliverable: the account has no password and does not need SMTP.

`scripts/prepare_staging_qa.py` provisions it only with explicit `--provision`.
The staging deploy workflow provisions once when that script is first added;
later deployments reuse the same identity without restoring deleted or revoked
assignments. Manual dispatch supports `qa_admin: provision`, `test`, or `off`.
Never substitute an existing person's identity or give this account production
access. Club staff controls can revoke its three assignments when no longer needed.

The workflow uses the existing staging Auth Admin secret to generate and redeem
a sign-in token without email. The resulting one-hour session is kept inside
the runner; no token, password, service key or browser storage is included in
artifacts. An always-running cleanup ends refreshability after the test. Access
JWTs can remain valid until expiry. Vercel's existing CI protection secret grants
the runner access to the attested deployment; the application still authenticates
and authorizes the real Supabase session normally.

`apps/web/e2e/multiclub-qa.staging.spec.ts` checks the real club selector, cookie
context, separate player IDs, website draft visibility controls, share URLs,
statistics controls, preview, denied access to Tres, and sign-out. It opens every
available interclub season from each test club and verifies season/meet requests,
invitation controls, participant scoping, and organizer visibility. It does not
accept invitations, change rosters, save drafts or publish pages. It also checks that club creation starts with club
details, preserves them on refresh, fits a phone viewport, and asks a signed-in
administrator to confirm their account in Step 2 before review. It stops before creating a new club so the QA
account retains its three-club boundary. The exact-candidate handoff includes `qa-account.json`
and `qa-browser.json` after a successful run. This is automated browser acceptance;
interactive browser access still requires the browser's secure sign-in flow.

Independent single-club-account acceptance remains a separate manual check.
The automated authorization tests cover those permission rules; this QA identity
intentionally has access to the three fixture clubs for switching tests.
